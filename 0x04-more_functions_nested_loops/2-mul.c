#include "main.h"

/**
* mul - a function check for upper letter @a and @b
* @a: a character to be checked
* @b: also checked
* Return: 0 if not amd 1 if upper
*/

int mul(int a, int b)
{
	int res;

	res = a * b;
	_putchar(res + '0');
}
